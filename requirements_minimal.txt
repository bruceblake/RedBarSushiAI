# Core WebSocket requirements for Twilio Media Streams integration
# Only used if full requirements installation is failing
flask==3.1.0
flask-sock==0.7.0
gevent==23.9.1
gevent-websocket==0.10.1
gunicorn==23.0.0
websockets==13.1

# OpenAI and its dependencies
openai>=1.0.0,<2.0.0
anyio>=3.6.2,<4.0.0
httpx>=0.24.1

# Database and caching
psycopg2-binary==2.9.9
redis==5.2.1

# Audio processing
ffmpeg-python==0.2.0
pydub==0.25.1  # Audio processing without PyAudio dependency
# PyAudio is commented out as it requires portaudio system dependencies
# Uncomment if portaudio19-dev is installed on the system
# pyaudio==0.2.14

# Async task processing
celery==5.4.0

# Voice processing
twilio==9.4.6