name: Test and Lint

on:
  push:
    branches: [ main, staging ]
  pull_request:
    branches: [ main, staging ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install -r requirements.txt
        python -m pip install -r requirements.ci.txt
        python -m pip install black ruff
        
    - name: Set up test environment
      run: |
        # Create test config file
        cp .env.test.example .env.test || true
        python -c "import os; f=open('.env.test', 'w'); f.write('DATABASE_URL=sqlite:///:memory:\nFLASK_ENV=testing\nTESTING=true\nLOG_LEVEL=DEBUG\nDISABLE_OPENAI=true\nDISABLE_TWILIO=true\nDISABLE_DELIVERECT=true\nSQLALCHEMY_TRACK_MODIFICATIONS=false'); f.close()"
        
    - name: Check code formatting with Black
      run: |
        # Check if files are formatted properly, but don't fail the build
        black --check app tests || echo "Black formatting issues found, please run 'black app tests' locally"
        
    - name: Lint with flake8 and ruff
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
        # Run ruff for additional checks but don't fail the build
        ruff check app tests --exit-zero || echo "Ruff linting issues found"
        
    - name: Run unit tests
      env:
        TESTING: "True"
        DISABLE_OPENAI: "True"
      run: |
        pytest -m "not integration and not load" -v || { TEST_EXIT_CODE=$?; if [ $TEST_EXIT_CODE -eq 5 ]; then echo "No unit tests found, continuing..."; exit 0; else exit $TEST_EXIT_CODE; fi; }
        
  integration-test:
    runs-on: ubuntu-latest
    # Only run integration tests on main and staging branches 
    # and only if explicitly triggered or on a scheduled run
    if: github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '[run-integration]')
    needs: test
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install -r requirements.txt
        python -m pip install -r requirements.ci.txt
        
    - name: Set up test environment
      run: |
        cp .env.test.example .env.test || true
        python -c "import os; f=open('.env.test', 'w'); f.write('DATABASE_URL=sqlite:///:memory:\nFLASK_ENV=testing\nTESTING=true\nLOG_LEVEL=DEBUG\nSQLALCHEMY_TRACK_MODIFICATIONS=false'); f.close()"
        
    - name: Run integration tests
      env:
        TESTING: "True"
        DISABLE_OPENAI: "True"
      run: |
        pytest -m "integration" -v || { TEST_EXIT_CODE=$?; if [ $TEST_EXIT_CODE -eq 5 ]; then echo "No integration tests found, continuing..."; exit 0; else exit $TEST_EXIT_CODE; fi; }