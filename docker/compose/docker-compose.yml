services:
  app:
    build:
      context: ../../
      dockerfile: docker/images/Dockerfile
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/redbarsushi
      - POSTGRES_PASSWORD=postgres
      - FORCE_HEADLESS=true
      - OPENAI_REALTIME_NO_DISPLAY=1
      - CONTAINER_MODE=1
      - VOICE_HANDLER=realtime
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - ../../app:/app/app
      - ../../logs:/app/logs
      - ../../init_database.py:/app/init_database.py
      - ../../menu_data.json:/app/menu_data.json
    networks:
      - redbarsushi-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  postgres:
    image: postgres:14
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=redbarsushi
    ports:
      - "${POSTGRES_PORT:-5433}:5432"  # Changed from 5432 to 5433 on host
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ../../db/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - redbarsushi-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  redis:
    image: redis:6
    ports:
      - "${REDIS_PORT:-6380}:6379"  # Changed from 6379 to 6380 on host
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - redbarsushi-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  redbarsushi-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data: