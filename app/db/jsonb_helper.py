"""
Helper module for determining the appropriate JSON type based on the database dialect.
"""

# The get_json_type function was here, but removed as it was flagged as unused by Vulture.
# All imports (os, logging, sqlalchemy.Text, app.config.settings) became unused
# as a result and have been removed.
# This file is now empty. It could be deleted if no other part of the application
# references it (e.g. for type checking or future use).
